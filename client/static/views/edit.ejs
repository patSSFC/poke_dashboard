<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Edit Form</title>
        <link href="https://fonts.googleapis.com/css?family=Slabo+27px" rel="stylesheet">
        <link rel="stylesheet" href="/styles/index.css">
    </head>
    <body>
        <form action="/pokemon/:name" method="post" id="poke-form">
            <fieldset>
                <% for (var x in poke_list)  { %>
                <div class="input-contain">
                    <label for="poke-name">Pokemon Name:</label>
                    <input id="poke-name" type="text" name="name" value=<%= poke_list[x].name%>>
                </div>
                <div class="input-contain">
                    <label for="poke-type">Pokemon Type:</label>
                    <input id="poke-type" type="text" name="type" value=<%= poke_list[x].type%>>
                </div>
                <div class="input-contain">
                    <label for="poke-weight">Pokemon Weight (in KG):</label>
                    <input id="poke-weight" type="text" name="weight" value=<%= poke_list[x].weight%>>
                </div>
                <div class="input-contain">
                    <label for="poke-height">Pokemon Height (in M):</label>
                    <input id="poke-height" type="text" name="height" value=<%= poke_list[x].height%>>
                </div>
                <button type="submit" form="poke-form" value="Submit">Submit</button>
                <% } %>
            </fieldset>
        </form>
    </body>
</html>
