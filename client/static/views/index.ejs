<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Pokemon DB</title>
        <link href="https://fonts.googleapis.com/css?family=Slabo+27px" rel="stylesheet">
        <link rel="stylesheet" href="../styles/index.css">
    </head>
    <body>
        <form action="/users" method="POST" id="survey-form">
            <fieldset>
                <% for (var x in poke_list)  { %>
                <p><%= poke_list[x].name %></p>
                <p><%= poke_list[x].type %></p>
                <p><%= poke_list[x].weight %></p>
                <p><%= poke_list[x].height %></p>
                <hr>
                <% } %>
            </fieldset>
        </form>
    </body>
</html>
